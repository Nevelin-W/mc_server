#cloud-config
package_update: true
package_upgrade: true

packages:
  - fail2ban
  - htop
  - wget
  - curl
  - unzip
  - jq

runcmd:
  # Disable UFW – cloud provider firewall handles port filtering
  - ufw disable || true
  - systemctl enable fail2ban
  - systemctl start fail2ban
